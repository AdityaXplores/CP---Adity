#include "bits/stdc++.h"
using namespace std;

void solve() {
    int n; cin >> n;
    string str; cin >> str;
    vector<int> a(n);
    vector<long long> c(n);
    for(auto &num : a) cin >> num;
    for(auto &num : c) cin >> num;
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t; cin >> t;
    while(t--){
        solve();
    }
}